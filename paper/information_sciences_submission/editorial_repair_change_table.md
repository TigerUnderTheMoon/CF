# Editorial Repair Change Table

| Category | Requested issue | Repair applied | Verification |
|---|---|---|---|
| Author metadata | Title page rendered `Haoran Maa`. | Corrected rendered author name to `Haoran Ma`; preserved affiliation markers. | Page 1 visual check and PDF text scan show `Haoran Ma`; no `Haoran Maa`. |
| Figure 1 garbage text | Figure 1 contained the embedded garbage line `Output: fixed-budget pr Ly, an bottleneck fields`. | Cleaned the Figure 1 bitmap and completed the caption ending with the recommended review action. | Page 2 visual check shows no embedded output line; scan found no `Output:`/`Dutput` garbage. |
| Duplicate section heading | Section `2.2. Process Supervision and Process Reward Models` appeared twice/malformed. | Removed the duplicate malformed heading and retained one valid Section 2.2 heading. | PDF text count for the Section 2.2 heading is `1`. |
| O/0 confusions | Several table and appendix entries used `O` where `0` was intended, or `0` where complexity `O` was intended. | Corrected `PRM800K`, `SC-FMA QP`, `SC-FMA Ridge`, `0.5749`, `0.91-0.97`, `prm800k_pool_006217`, `[0.5, 1.0, 0.0]`, `rho = -1.0`, and complexity notation `O(k^2)`, `O(k)`, `O(Ik^3)`. | Checklist scan found no requested bad O/0 strings; Table 1/2 and Tables 9/10/16/17 render with corrected forms. |
| LaTeX/math formatting | Formula and notation issues included spaced `l o g`, malformed graph definition, `\mathrm{O P}is`, broken `If`, and corrupted sample count. | Normalized the SCU objective to `\log(w_i)` and `w^\top R w`; repaired `G = (V, E)`/`G := (V, E)`-style notation, `QP is`, `If \alpha + \beta > 0`, and `4,417 samples`. | Source/PDF scan found no listed malformed LaTeX snippets. |
| Punctuation and spacing | Punctuation, table/figure/section references, theorem/limitation labels, numeric quantities, and percent metrics had missing spaces. | Normalized sentence punctuation, number-word spacing, `Figure/Table/Section` references, parenthetical labels, and metric contexts such as `Coverage NDCG@25% of`. | Regex scan found no requested joined-spacing patterns. |
| Terminology and spelling | Misspellings included `retrievalaugmented`, `KGassisted`, `SCFMA`, `TFIDF`, `scalaronly`, `fixedbudget`, `variantselection`, `domainspecific`, and `best-of-Nsearch`. | Standardized to `retrieval-augmented`, `KG-assisted`, `SC-FMA`, `TF-IDF`, `scalar-only`, `fixed-budget`, `variant selection`, `domain-specific`, and `best-of-N search`; used `process supervision` consistently. | Source/PDF scan found none of the requested bad variants; visual checks confirmed readable line breaks. |
| Code/variable leakage in prose | Body text contained `kbs_style_audit_prioritization_evidence_only` and appendix text contained `real_prm800k_audit_prioritization_only`. | Replaced rendered prose with natural-language claim-boundary statements while keeping the source-only machine-readable marker as a LaTeX comment for verifier compatibility. | PDF text contains no code-variable leakage; the remaining marker is only in a LaTeX comment. |
| Truncated paragraphs | Page-boundary fragments were reported in the abstract, Introduction, Figure 1 caption, Sections 2.1/2.2, 3.4, 5.3, 6.2, Conclusion, appendix, and references. | Completed affected sentences and added a page-break guard before the Introduction contributions list. | Visual checks of pages 1, 2, 14-17, 21, and 22 show complete paragraph/sentence endings; Conclusion ends with `knowledge-intensive reasoning systems.` |
| References | Reference [27] had URL/DOI crowding and page-break artifacts; [35] was reported as truncated. | Rebuilt bibliography, removed the trailing slash from the ACL URL to avoid the isolated `l-long.510/` artifact, used ragged-right bibliography layout, and verified [35] renders fully. | PDF scan found no `l-long.510/`; pages 21-22 show complete [27] and [35] entries. |
| Data Availability line breaks | Long `\path{}` entries visually produced `ge nerate` and `validati on`-like splits. | Rephrased the reproducibility sentence with short file names and natural directory descriptions. | Page 17 visual check and PDF text extraction show `generate_synthetic_traces.py` and `auto-validation` without internal spelling breaks. |
| Upload package synchronization | Source edits can leave `final_package` stale. | Recompiled the source PDF and prepared to refresh `manuscript.pdf` and `latex_source.zip` in the five-file upload boundary. | Final package verification is recorded separately after package rebuild. |
